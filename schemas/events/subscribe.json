{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Subscribe",
  "description": "Subscribe to custom events via WebSocket",
  "type": "object",
  "required": ["events"],
  "properties": {
    "events": {
      "type": "array",
      "description": "List of event names to subscribe to",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100
      },
      "minItems": 1,
      "uniqueItems": true,
      "examples": [
        ["user_status_changed"],
        ["relay_status_change", "instance_created"],
        ["message_received"]
      ]
    }
  },
  "additionalProperties": false
}
