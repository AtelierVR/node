{
    "$id": "relations/update_response",
    "title": "Relation Update Response Schema",
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "enum": [
                "follow_accepted",
                "follow_rejected",
                "follow_pending",
                "unfollow_accepted",
                "error",
                "ok"
            ]
        },
        "retryAfter": {
            "type": "integer",
            "minimum": 0
        },
        "note": {
            "type": "string",
            "maxLength": 500
        }
    },
    "required": [
        "type"
    ],
    "additionalProperties": true
}